spring:
  application:
    name: api-gateway
  cloud:
    bootstrap: true
    config:
      discovery:
        enabled: true
        service-id: config-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9105/eureka
