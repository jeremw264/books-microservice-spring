spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: [classpath:/config, file:./config]
server:
  port: 9190

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9105/eureka/
